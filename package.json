{
  "name": "telegram-file-share-bot",
  "version": "1.0.0",
  "description": "A Telegram bot that uses a group as a file storage system. When a user uploads a file to the group, the bot replies with a public link. You can then share this link, and anyone who clicks on it will receive the file through the bot, making file sharing easy and efficient.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antoine Peña (antoinepdev)",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.67.0"
  }
}
